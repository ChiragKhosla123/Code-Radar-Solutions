#include <stdio.h>

int main() {
    int a[100],b,s;
    scanf("%d",&b);
    for(int i=0;i<b;++i){
        scanf("%d",&a[i]);
    if(a[i]<s[i]){
        printf("%d ",a[i]);
        }
    else{
        printf("%d",a[i]);
        }
    }
    return 0;
}