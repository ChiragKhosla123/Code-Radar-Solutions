#include <stdio.h>

int main() {
    int n,a[100],s,g[100];
    scanf("%d",&n);
    for(int i=0;i<n;++i){
        scanf("%d",&a[i]);
    }
    scanf("%d",&s);
    printf("%d",g[s]);
    return 0;
}